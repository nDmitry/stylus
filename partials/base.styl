html {
  background: #fff;
  font-family: base-font-family;
  color: base-text-color;
}

button,
input,
select,
textarea {
  color: base-text-color;
}

hr {
  display: block;
  height: 1px;
  border: 0;
  border-top: #ccc;
  margin: 1em 0;
  padding: 0;
}

a {
  link-colors(blue, lighten(blue, 30%));
}
